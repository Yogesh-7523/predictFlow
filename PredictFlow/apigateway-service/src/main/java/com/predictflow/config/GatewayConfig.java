package com.predictflow.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
    // Gateway route definitions are in ApiGatewayApplication (RouteLocator).
    // Add beans here later for rate limiting, custom filters, etc.
}